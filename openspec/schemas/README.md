# openspec/schemas

ローカルにフォークしたワークフロースキーマを置く場所です。`openspec schema fork` で作成し、`schema.yaml`・`templates/` を編集してカスタマイズします。

`spec-driven/` は組み込みスキーマ `spec-driven` を同名でローカル fork したもので、テンプレートにこの kit のルールファイルへの参照ポインタを埋め込んでいます（ルール本文はコピーしていません）。
