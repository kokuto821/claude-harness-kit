# shared-rules

任意のタイミングで参照する**共通ルール（参照層）**を置く場所です。トピックごとのサブディレクトリに `<短縮トピック>-rule.md` としてまとめ、スキル・サブエージェントから `[[link]]` で必要時に参照します（自動ロードはされません）。

matagi 自体（プロンプト・コンテキスト設計・ハーネス構成）のレビュー・改善基準を置きます。`[[link]]` 記法自体の定義・コア（`rules/`）は `matagi` プラグイン側にあります。
