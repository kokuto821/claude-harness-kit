# frontend-code-review

フロントエンドの変更を、コーディング規約・テスト規約・UIデザインの3観点でまとめてレビューする統合オーケストレーターのスキルです（指摘のみ）。

定義は `SKILL.md`。各観点は `frontend-coder` / `frontend-tester` / `ui-designer` エージェントへ委譲します。改善の適用まで行う場合は `frontend-coding` / `ui-review`（承認後 `ui-designer` 修正モード）を使います。
