# record-all

会話内容を、ナレッジ蓄積（`record-knowledge` → `knowledge/`）とフィードバックのルール化（`record-feedback` → `rules/`）の両方でまとめて記録する統合スキルです。

定義は `SKILL.md`。各処理は既存の `record-knowledge` / `record-feedback` の手順・判断基準を踏襲します。
