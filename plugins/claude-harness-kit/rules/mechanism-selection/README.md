# mechanism-selection

Claude Code のステアリング手法（CLAUDE.md / rules / skills / subagents / output styles / append）の選び方に関するルールを置く場所です。

Markdown・指示系の中でどの手法を使うかの判断基準を `selection-rule.md` にまとめています。コード vs Markdown の一次判定は `harness-control/` を参照。
