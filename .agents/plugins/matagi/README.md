# matagi (Antigravity plugin)

matagi を Google Antigravity プラグインとして配置したものです。`skills`/`rules`/`agents`/`hooks` は `plugins/matagi/` への symlink、`plugin.json`/`hooks.json` は変換スクリプトの生成物（git 管理外）です。中身を直接編集しないでください。source of truth は `plugins/matagi/` です。
